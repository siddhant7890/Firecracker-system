steps:
  # Step 1: Fetch Go module dependencies (fails fast before App Engine's own build)
  # Step 2: Deploy to App Engine
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['app', 'deploy', '/app.yaml']

options:
  logging: CLOUD_LOGGING_ONLY