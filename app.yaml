runtime: go126
env: standard
service: server
instance_class: F1

# Only needed if your main package isn't in the same folder as this file
# (e.g. your entrypoint is cmd/api/main.go, not server/main.go).
main: ./cmd/api